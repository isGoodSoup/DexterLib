# DexterLib

DexterLib is a small personal Java utility library containing handy non-static helper methods.  
It’s meant to be a simple toolbox you can reuse across projects without rewriting common logic.

## Features
- Lightweight, no dependencies  
- Non-static utility methods  
- Methods available in English and Spanish  
- Easy to drop into any Java project  

## Installation
Clone the repo and include the `utils` package in your project:

```bash
git clone https://github.com/isGoodSoup/DexterLib
```